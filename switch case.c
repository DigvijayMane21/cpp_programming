#include<stdio.h>
int main()
/* simple calculator
{
    printf("Press + for addition, Press - for substraction, Press * for multiplication, Press / for division.\n");
    printf("Press ? to get remainder of the division, Press $ to get percentage.\n");
    char ch;
    int x,y;
    printf("Enter first number followed by an operator followed by next number:");
    scanf("%d %c%d",&x,&ch,&y);
    switch (ch)
    {
        case '+':
            printf("%d + %d = %d",x,y,x+y);
            break;
        case '-':
            printf("%d - %d = %d",x,y,x-y);
            break;
        case '*':
            printf("%d * %d = %d",x,y,x*y);
            break;
        case '/':
            printf("%d / %d = %d",x,y,x/y);
            break;
        case '?':
            printf("%d ? %d = %d",x,y,x%y);
            break;
        case '$':
            printf("%d $ %d = %d",x,y,((y/x)*100));
            break;
        default:
            printf("Invalid Operator.");
            break;
    }
    return 0;
}
*/


/* code to determine the day of the given date of a month.
{
    int n;
    printf("Enter a date:");
    scanf("%d",&n);
    n=n%7;
    switch (n)
    {
        case 0:
            printf("sunday");
            break;
        case 1:
            printf("monday");
            break;
        case 2:
            printf("tuesday");
            break;
        case 3:
            printf("wednesday");
            break;
        case 4:
            printf("thursday");
            break;
        case 5:
            printf("friday");
            break;
        case 6:
            printf("saturday");
            break;
        default:
            break;
    }
    return 0;
}
*/
